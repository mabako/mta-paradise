<!-- based on 'traffic' by lil_toady and Flobu
  -- from http://flobu.fl.funpic.de/MTA/traffic.rar
  -->
<meta>
	<script src="unlinked.lua" />
	<script src="closestnode.lua" />
	
	<!-- Returns the closest vehicle path node to a given point.
		parameters:
			(number) x-position of the point you want to find a close node to
			(number) y-position                     "
			(number) z-position                     "
		returns:
			(number) x-position of the nearest node
			(number) y-position         "
			(number) z-position         "
	-->
	<export function="findClosest"/>
</meta>